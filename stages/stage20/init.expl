int main(){
decl
	int temp;
	str processname;
enddecl
begin
    temp = exposcall("Write",-2,"Enter process name:");
    temp = exposcall("Read",-1,processname);
    temp = exposcall("Exec",processname);
    temp = exposcall("Write",-2,"Not executable");
	return 0;
end
}